[workspace]
resolver = "2"
members = [
    "kernel",
    "vexfs",
    # "ai-core",   # future member
    # "compositor", # future member
    # "shell",      # future member
]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
opt-level = 3
lto = true
codegen-units = 1
